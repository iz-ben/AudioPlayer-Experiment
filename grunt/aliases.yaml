default:
  description: 'Compile javascript files'
  tasks:
    - 'closureBuilder'
createmanager:
  description: 'Compile javascript files'
  tasks:
    - 'closureBuilder:createAudioManager'
createplayer:
  description: 'Compile javascript files'
  tasks:
    - 'closureBuilder:createAudioPlayer'